name: tp2
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.6.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.3.0  # Ajoutez cette ligne pour la bibliothèque HTTP
  image_picker: ^1.1.2  # Assurez-vous d'avoir aussi cette bibliothèque si vous l'utilisez

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
